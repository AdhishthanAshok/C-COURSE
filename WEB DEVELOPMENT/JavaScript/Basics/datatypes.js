"use strict"; // treat all js code as newer version --> Nowadays it is inderstood so no need to write this .. but it is a good practice ..

// alert(3+3)   --> can not be used in node as like this .. can be iinly used in browser console .. because it is an document. .. we can use alert in node with different syntax;

// We avoid semicolon in react and node.
