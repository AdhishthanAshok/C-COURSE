#include<bits/stdc++.h>
using namespace std;

int main(){
    int size ;
     cout << "Enter the size of the array : ";
    cin >> size; 
    array <int,10> arr; 

    for(int count : arr ){
        cout << count << "  " ;
    }
    
    return 0;
}